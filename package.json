{
  "name": "shop",
  "scripts": {
    "build": "npm i -w frontend && npm run build -w frontend && npm i -w backend && npm run build -w backend && cp -r frontend/dist backend/dist/frontend-dist",
    "start": "node backend/dist/app.js"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
